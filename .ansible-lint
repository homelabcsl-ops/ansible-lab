# vim:ft=yaml
---
# DevOps Standard Ansible Linting Configuration
profile: production

# Warn instead of fail for experimental rules
warn_list:
  - experimental

# Explicitly declare which directories contain which file types
parseable: true
